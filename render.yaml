services:
  - type: web
    name: ds2api
    env: docker
    plan: free
    region: singapore
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5001
      - key: DS2API_ADMIN_KEY
        generateValue: true
      - key: DS2API_CONFIG_PATH
        value: 'config.json'
